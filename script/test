#!/usr/bin/env bash

if [ "$POD_FEATURE_POSTGRESQL" = "true" ]
then
    clojure -A:test -n pod.babashka.postgresql-test
fi

if [ "$POD_FEATURE_HSQLDB" = "true" ]
then
    clojure -A:test -n pod.babashka.hsqldb-test
fi
